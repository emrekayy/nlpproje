# Raw Review CSVs

Put external review datasets here as `.csv` files.

Required columns:

- `id`
- `model`
- `source`
- `rating`
- `review_text`
- `date`

These files are treated as raw input and normalized by the API preprocessing pipeline.
